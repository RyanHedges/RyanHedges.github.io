
<% page_articles.each_with_index do |article, i| %>
  <article class="row" data-file-path=<%= article.destination_path %> data-target-element="article" >
    <h2 class="col-xs-12 article-link"><%= link_to article.title, article %></h2>

    <time class="col-xs-12 col-sm-7"><%= article.date.strftime('%A %B %e, %Y') %></time>
    <span class="eta col-sm-5"></span>

    <span class="col-xs-12" aria-label="Tags">
      <span class="glyphicon glyphicon-tags" aria-hidden="true"></span>
        <% article.tags.each do |tag| %>
          <%= tag_link(article, tag) %>
        <% end %>
    </span>

    <div class="article-summary">
      <%= article.summary %>
      <%= link_to 'Keep reading...', article %>
    </div>
  </article>
<% end %>


<nav class="row">
  <div class="col-xs-12">
    <ul class="pager">
      <li class="previous <%= pager_disabled(prev_page) %>"><%= link_to 'newer', prev_page %> </li>
      <li><span class="page-location"><%= page_number %> of <%= num_pages
%></span></li>
      <li class="next <%= pager_disabled(next_page) %>"><%= link_to 'older', next_page %> </li>
    </ul>
  </div>
</nav>

